*add that when program starts the value of null so first agent should be selected. - DONE

/*****writing to file
					//  FileWriter fstream = new FileWriter("xmlFile/0.xml");
			     	//   BufferedWriter out = new BufferedWriter(fstream);
			        System.out.println("printing file");
			        	//  out.write("Hello Zero");
			        //Close the output stream
			        	//   out.close();
				 ***/
				 
* collect data structures from xml file		DONE	 
* add window for data structures DONE
* checks for int string double
* write random functions for generating random values
* identfy arrays and take inputs accoringly



***scan user input on columns 1 and 3
JTextField textField = new JTextField();
        textField.addKeyListener(new java.awt.event.KeyAdapter(){
            
            public void keyPressed(java.awt.event.KeyEvent evt){
                System.out.println("keyPressed");
            }
              
            public void keyReleased(java.awt.event.KeyEvent evt){
                System.out.println("keyReleased");
            }
              
            public void keyTyped(java.awt.event.KeyEvent evt) {
                System.out.println("keyTyped");
            }
        });
        table.getColumnModel().getColumn(1).setCellEditor(new DefaultCellEditor(textField));
        table.getColumnModel().getColumn(3).setCellEditor(new DefaultCellEditor(textField));


*recursive data type half done
need to pass instance of text field to portray result
if table do this
if window display in text field

11/3/08
===================================================================
* list of functions to be provided by the id
===================================================================
random() - create a random number within range a and b
auto for values that will be generated by id - dynamic arrays, ids
randomInt(a,b)
randomDouble(a,b)
random values for everyagent specifically()Int and double
checking validity of variables

======================================================================
Publishing Notes:
======================================================================
Important to clean the model.xml before putting it through the program
List of functions provided

============================================================
history 
============================================================
11/3/08
corrected the opening problem of null agent selection
added text area
added check for numeric value for number of agents

13/03/08
added random number function between two numbers function randomInt(a,b)
added random number generation randomDouble(a,b)
added extra frame thing
hope to create a table for inputing values for array and data structures both. 
this would reduce the created of small windows

17/03/08
added the inputing values for arrays bit
have to get column names for data structures in a vector class. to send across

18/03/08
added data structure and array entering in a seperate list
TODO-have to add nested ds

19/03/08
*added nested data types
*corrected problem for one row entering of data structures
*added file input through dialog box
*added for nested arrays.
* corrected the looping array problem

20/03/08
make jar file
put up on the wiki
added new functions for producing random values for every agent on the same row

