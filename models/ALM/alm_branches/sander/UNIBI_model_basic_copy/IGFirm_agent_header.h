/**
 * \file  IGFirm_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def ID
 * \brief Direct access to id of IGFirm agent memory variable. */
#define ID current_xmachine->xmachine_IGFirm->id
/** \def REGION_ID
 * \brief Direct access to region_id of IGFirm agent memory variable. */
#define REGION_ID current_xmachine->xmachine_IGFirm->region_id
/** \def PRODUCTIVITY
 * \brief Direct access to productivity of IGFirm agent memory variable. */
#define PRODUCTIVITY current_xmachine->xmachine_IGFirm->productivity
/** \def INNOVATION_PROBABILITY
 * \brief Direct access to innovation_probability of IGFirm agent memory variable. */
#define INNOVATION_PROBABILITY current_xmachine->xmachine_IGFirm->innovation_probability
/** \def PRODUCTIVITY_PROGRESS
 * \brief Direct access to productivity_progress of IGFirm agent memory variable. */
#define PRODUCTIVITY_PROGRESS current_xmachine->xmachine_IGFirm->productivity_progress
/** \def CAPITAL_GOOD_PRICE
 * \brief Direct access to capital_good_price of IGFirm agent memory variable. */
#define CAPITAL_GOOD_PRICE current_xmachine->xmachine_IGFirm->capital_good_price
/** \def REVENUE_PER_DAY
 * \brief Direct access to revenue_per_day of IGFirm agent memory variable. */
#define REVENUE_PER_DAY current_xmachine->xmachine_IGFirm->revenue_per_day
/** \def DAY_OF_MONTH_TO_ACT
 * \brief Direct access to day_of_month_to_act of IGFirm agent memory variable. */
#define DAY_OF_MONTH_TO_ACT current_xmachine->xmachine_IGFirm->day_of_month_to_act
/** \def RANGE
 * \brief Direct access to range of IGFirm agent memory variable. */
#define RANGE current_xmachine->xmachine_IGFirm->range
/** \def POSX
 * \brief Direct access to posx of IGFirm agent memory variable. */
#define POSX current_xmachine->xmachine_IGFirm->posx
/** \def POSY
 * \brief Direct access to posy of IGFirm agent memory variable. */
#define POSY current_xmachine->xmachine_IGFirm->posy
