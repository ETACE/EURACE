/**
 * \file  Bank_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def ID
 * \brief Direct access to id of Bank agent memory variable. */
#define ID current_xmachine->xmachine_Bank->id
/** \def PROPOSED_INTEREST_RATE
 * \brief Direct access to proposed_interest_rate of Bank agent memory variable. */
#define PROPOSED_INTEREST_RATE current_xmachine->xmachine_Bank->proposed_interest_rate
/** \def AMOUNT_CREDIT_OFFER
 * \brief Direct access to amount_credit_offer of Bank agent memory variable. */
#define AMOUNT_CREDIT_OFFER current_xmachine->xmachine_Bank->amount_credit_offer
/** \def RANGE
 * \brief Direct access to range of Bank agent memory variable. */
#define RANGE current_xmachine->xmachine_Bank->range
/** \def POSX
 * \brief Direct access to posx of Bank agent memory variable. */
#define POSX current_xmachine->xmachine_Bank->posx
/** \def POSY
 * \brief Direct access to posy of Bank agent memory variable. */
#define POSY current_xmachine->xmachine_Bank->posy
